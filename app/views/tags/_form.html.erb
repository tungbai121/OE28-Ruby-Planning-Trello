<%= form_for @tag, url: board_create_tag_path(@board) do |f| %>
  <%= f.text_field :name, placeholder: t(".enter_name") %>
  <%= hidden_field_tag :list_id, list.id %>
  <%= f.submit t(".add"), class: "btn btn-primary" %>
<% end %>
