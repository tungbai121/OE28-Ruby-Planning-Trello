<div class="label">
  <div class="content">
    <%= label.content %>
  </div>
  <%= link_to t(".labeldelete"),
    board_tag_labels_path(@board, label_id: label.id, tag_id: tag_id),
    method: :delete,
    class: "delete",
    remote: true %>
</div>
